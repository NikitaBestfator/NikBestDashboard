<UserControl x:Class="NikBestDashboard.Modules.SearchModule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="🔍 Глобальный поиск" FontSize="24" FontWeight="Bold" Foreground="#EEEEFF" Margin="0,0,0,15"/>

        <!-- Поисковая строка -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBox x:Name="SearchTextBox" Text="Введите слово для поиска..." Foreground="#888888" Background="#2A2A3E" BorderBrush="#444466" Padding="10" FontSize="14" 
                     GotFocus="SearchTextBox_GotFocus" LostFocus="SearchTextBox_LostFocus" KeyDown="SearchTextBox_KeyDown" TextChanged="SearchTextBox_TextChanged"/>
            <TextBlock x:Name="ResultsCountText" Foreground="#8888AA" FontSize="12" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Результаты -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ResultsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2A2A3E" CornerRadius="8" Padding="12" Margin="0,0,0,8" BorderBrush="#444466" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding ModuleIcon}" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Title}" Foreground="#EEEEFF" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding ModuleName}" Foreground="#C9B1FF" FontSize="11"/>
                                        <TextBlock Text=" • " Foreground="#666688" FontSize="11"/>
                                        <TextBlock Text="{Binding Category}" Foreground="#8888AA" FontSize="11"/>
                                        <TextBlock Text=" • " Foreground="#666688" FontSize="11" Visibility="{Binding Tags, Converter={StaticResource StringToVisibilityConverter}}"/>
                                        <TextBlock Text="{Binding Tags}" Foreground="#8888AA" FontSize="11"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Description}" Foreground="#8888AA" FontSize="12" TextWrapping="Wrap" Margin="0,2,0,0" Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>

                                <Button Grid.Column="2" Content="➡" Click="NavigateToModule_Click" Tag="{Binding}" Background="#7C4DFF" Foreground="White" FontWeight="Bold" Padding="10,5" BorderThickness="0" Cursor="Hand" ToolTip="Перейти" Margin="10,0,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>