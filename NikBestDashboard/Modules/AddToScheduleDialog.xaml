<Window x:Class="NikBestDashboard.Modules.AddToScheduleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📅 Добавить в план" Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📅 Добавить в планировщик" FontSize="18" FontWeight="Bold" Foreground="#EEEEFF" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Тема:" Foreground="#CCCCDD" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBlock x:Name="TitleText" Grid.Row="2" Foreground="#EEEEFF" FontSize="14" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="✅ Добавить" Click="AddButton_Click" Background="#7C4DFF" Foreground="White" FontWeight="Bold" Padding="15,8" BorderThickness="0" Cursor="Hand" Width="100"/>
            <Button Content="❌ Отмена" Click="CancelButton_Click" Background="#444466" Foreground="White" FontWeight="Bold" Padding="15,8" BorderThickness="0" Cursor="Hand" Width="100" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>